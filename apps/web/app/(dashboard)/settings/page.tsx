import { PagePlaceholder } from '@/components/shared/page-placeholder';

export default function Page() {
  return <PagePlaceholder title="Settings" description="Profile, sessions, and preferences." />;
}
